<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:invoices.front.ViewModels"
        x:Class="invoices.front.Views.LoginWindow"
        x:DataType="vm:LoginViewModel"
        Title="Trash Invoices - Login"
        Width="400" Height="340"
        WindowStartupLocation="CenterScreen"
         CanResize="False"
         Background="#1A1A27">
    <Grid RowDefinitions="*,Auto" Margin="20">
        <StackPanel Grid.Row="0" Spacing="12" VerticalAlignment="Center">
            <TextBlock Text="Trash Invoices"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#7E6FFF"
                       HorizontalAlignment="Center"/>

            <TextBox PlaceholderText="Username"
                     Text="{Binding Username}" />

            <TextBox PlaceholderText="Password"
                     Text="{Binding Password}"
                     PasswordChar="●" />

            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="#F38BA8" FontSize="12" />

             <Button Content="Login"
                     Command="{Binding LoginCommand}"
                     IsEnabled="{Binding !IsLoading}"
                     Foreground="White"
                     Background="#7E6FFF"
                     HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</Window>
